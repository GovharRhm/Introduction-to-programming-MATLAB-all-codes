% Function to compute the taxi fare

function all=taxi_fare(d,t)

time=0.25*ceil(t);    % ceil(t) rounds to positive infinity
distance=5+2*(ceil(d)-1);
all=time+distance;

end

fare=taxi_fare(3.5,2.25)
